// Do while loops execute its statements at least once before testing if its condition returns `true` before executing its statements again until its tested condition returns false.

let count = 20;
do {
	count++;
	console.log(count);
} while (count < 5);

// The loop will run once and return 21 since it added +1 to the count variable during its first loop cycle.
