let namesArray = ["Will", "James", "Ben", "Adam", "Mary", "Reginald"];
let object = "New Item";

// Array methods:
// namesArray.unshift();			// Removes and returns the first item from an array.
// namesArray.shift(object); // Adds an array item to the start of the specified/chained array.
// .pop()										// Removes OR "pops off" and returns the last item from the specified/chained array.
// .push(object)						// Adds an new item to the end of an array.
// .reverse() 							// reverses the order of items contained within an array.
// .sort()									// sorts (in ascending numerical & then alphabetical order) the items contained within an array.
// .concat()								// Concatenates the items of one array to another array & returns the new concatenated array.
// .join()									// Joins the items contained within an array into an item.
// .slice()	  							// Creates a new array by slicing from a start index & ending before the specified second argument index.
// .splice()								// Creates a new array by splicing items from a start index to a specified number of indexes afterwards.
